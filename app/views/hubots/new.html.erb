<h1>Ready to build one?</h1>
<%= form_for @hubot, html: { class: 'form' } do |f| %>
  <fieldset>
    <%= f.label :name, "Name" %>
    <%= f.text_field :name, placeholder: 'hubot' %>
    <span class="help-block">What name your Hubot should respond to?</span>
    <%= f.submit 'Create', class: 'btn btn-primary' %>
  </fieldset>
<% end %>
